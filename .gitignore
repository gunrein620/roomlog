node_modules/
.pnpm-store/

.env
.env.*
!.env.example

.next/
next-env.d.ts
dist/
coverage/
*.tsbuildinfo
# 런타임 업로드 출력 디렉토리만 무시 — 전역 uploads/는 소스 라우트(app/api/trade/uploads)까지 잡아먹는다.
/uploads/
apps/api/uploads/
data/furniture-crawl/
apps/api/src/furniture-catalog/crawl-ikea.ts

npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
.run/
.superpowers/

.DS_Store
.cursor/
.worktrees/
worktrees/

# RoomLog-local runtime assets are intentionally kept outside normal Git history.
/services/mitunet/weights/*.pth
/services/mitunet/weights/*.pt
/runtime-assets/furniture-glb-dataset/

# 임시 데모 산출물 (GPU 재구성 드라이런, ~20MB — 좋은 캡처 나오면 room.spz로 대체)
apps/web/public/samples/dryrun.ply

# Claude Code 개인 로컬 설정 (커밋 금지)
.claude/settings.local.json

# Codex E2E 검증 스크래치(스크린샷 등)
artifacts/

# E2E 캡처 산출물 (18MB — 위 dryrun.ply와 같은 이유로 레포에 넣지 않는다)
apps/web/public/samples/room-e2e.spz
apps/web/public/samples/room-e2e.tuning.json

# Xcode 개인 IDE 상태 — 창 위치·열린 파일 같은 사람별 상태라 커밋하면 매번 충돌한다
**/xcuserdata/

# 실매물 도면 픽스처(로컬 확인 전용) — 커밋하지 않는다
apps/web/public/dev-fixtures/
