packages:
  - "apps/*"
  - "packages/*"

# 셸에는 네이티브 최적화 바이너리가 불필요하므로 빌드 스크립트를 건너뛴다.
# (sharp = next/image 최적화 옵션, unrs-resolver = JS 폴백 존재)
allowBuilds:
  '@nestjs/core': false
  sharp: false
  unrs-resolver: false
